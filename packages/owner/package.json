{
  "name": "@sorandomains/owner",
  "version": "0.8.0-migration.0",
  "description": "Run your Soran namespace on Stellar: configure claims, issue names and manage namespace policies.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "prepublishOnly": "tsc",
    "check:browser": "esbuild dist/index.js --bundle --platform=browser --external:@stellar/stellar-sdk --outfile=browser-check.js --legal-comments=none --log-level=error && node -e \"const fs=require('fs');const s=fs.readFileSync('browser-check.js','utf8');fs.unlinkSync('browser-check.js');if(/\\bBuffer\\b/.test(s)){console.error('FAIL: bare Buffer reference in browser bundle');process.exit(1)}console.log('browser bundle clean')\"",
    "test": "node --import tsx test/decode-failure.test.mts && node --import tsx --test test/integration.test.mts test/native-*.test.mts"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/SoranDomains/sdk.git",
    "directory": "packages/owner"
  },
  "homepage": "https://github.com/SoranDomains/sdk/tree/main/packages/owner#readme",
  "bugs": {
    "url": "https://github.com/SoranDomains/sdk/issues"
  },
  "keywords": [
    "stellar",
    "soroban",
    "soran",
    "name-service",
    "namespace",
    "issuance"
  ],
  "peerDependencies": {
    "@stellar/stellar-sdk": ">=17 <18"
  },
  "devDependencies": {
    "@stellar/stellar-sdk": "^17.0.1",
    "@types/node": "^22.20.1",
    "esbuild": "^0.25.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  }
}
